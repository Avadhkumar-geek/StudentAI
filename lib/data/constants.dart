import 'package:flutter/material.dart';

const Map<String, TextInputType> typeMap = {
  'text': TextInputType.text,
  'number': TextInputType.number,
};

const double cardAspectRatio = 0.88;

const kTransparent = Colors.transparent;
const kWhite = Colors.white;
const kBackGroundColor = Color.fromRGBO(234, 225, 215, 1);
const kChatAppBar = Color.fromRGBO(54, 54, 53, 1);
const kWhite70 = Colors.white70;
const kLightGreen = Color.fromRGBO(175, 225, 105, 1);
const kGreen = Colors.green;
const kForeGroundColor = Color.fromRGBO(100, 150, 255, 1);
const kBlue = Colors.blue;
const kButtonColor = Color.fromRGBO(241, 105, 26, 1);
const kOrange = Colors.orange;
const kRed = Colors.red;
const kCodeBgColor = Color.fromRGBO(42, 45, 48, 1);
const kDarkWhite = Color.fromRGBO(106, 106, 106, 1);
const kBlack = Colors.black;

const String formatter =
    'Format: This should be done silently. Please use appropriate markdown format to improve the readability and comprehension'
    ' of the response. Just format the response if needed. Do not write about format. Response should be natural and formated';
